version: 0.2

phases:

  install:
    commands:
      - apt-get update -y
      - apt-get install npm
      - apt-get install zip
      - npm i -g serverless
  build:
    commands:
      - ./build.sh
      - sls deploy --v